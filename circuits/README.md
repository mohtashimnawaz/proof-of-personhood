Circom circuit and helper scripts

- multiplier.circom: simple multiplier circuit proves a * b == ab
- input.json: sample inputs for the test

Scripts will be added under `scripts/circuits/` to compile and produce proofs using `circom` and `snarkjs`.